# 天鲜账本 V1.2.0 Phase5.1

## 进货动态商品行
每一种水果直接保留在页面中：

第一行：
水果 / 单位

第二行：
数量 / 总价 / 单件

点击“添加商品”，在下方增加下一组同样的输入框。
全部录入后统一保存整张进货单。

## 历史时间筛选
更多 → 历史记录支持：
全部、今天、昨天、近7天、近30天、本月、上月、自定义起止日期。

筛选范围统一作用于营业、进货、利润分配和采购计划历史。

## 版本
- versionCode: 13
- versionName: 1.2.0
- DB_VERSION: 6

## 签名
继续使用原来的：
- SIGNING_KEY_BASE64
- KEYSTORE_PASSWORD
- KEY_ALIAS

不要更换 keystore。

## V1.2.1 界面优化

- 首页、进货、营业、结算支持左右箭头切换前后一天。
- 首页日期可点击选择历史日期。
- 最近7天营业额趋势增加每日营业额数字。
- 更多页面将历史记录和经营统计合并为数据中心。
- 更多页面去掉功能卡片说明文字。
- 水果商品管理改名为商品管理。
- DB_VERSION 仍为 6。


## V1.2.2 经营分析与利润结算

- 经营分析支持全部/今天/昨天/近7天/近30天/本月/上月/自定义时间筛选。
- 结算拆分为当日结算、批量结算、结算统计。
- 每位合伙人每天的利润可独立确认是否已结算。
- 支持3～7天或任意范围批量选择合伙人统一结算。
- 结算统计显示应得、已结算、待结算、每日明细和期间合计。
- 新增结算批次，可撤销误确认。
- DB_VERSION 7。

## V1.2.3 报表功能

- 更多 → 生成报表。
- 支持利润分配、利润结算、经营汇总三类报表。
- 支持时间筛选、合伙人筛选、简洁版/详细版。
- 支持生成 PDF、PNG，并通过 Android 系统分享面板分享。
- 支持另存为 PDF / PNG。
- DB_VERSION 保持 7。

## V1.3.0 多账本与同步基础

- 一个账本使用一个独立 SQLite 数据库。
- 更多 → 账本管理：新建、切换、改名和删除本地账本。
- 现有数据自动归入默认“我的账本”。
- 每条业务数据增加 row_version / modified_by。
- 新增 sync_change_log，记录本机新增、修改和软删除，供后续增量云同步。
- 修复利润比例 0.33% 应显示 33.33% 的问题。
- DB_VERSION 升级到 8。

## V1.3.1 当前账本云同步

- 接入 `https://sync.830888.xyz` 天鲜同步服务器。
- 登录后自动注册设备。
- 当前账本使用本地 LedgerBook.id 原样创建云端 Book.id。
- 支持 sync_change_log 增量 push、cursor 增量 pull。
- DB_VERSION 9：增加 remote_apply 和 sync_cloud_state。
- 云端 pull 写入时抑制本地同步触发器，避免循环上传。
- 为未来多设备编辑预留设备独立 INTEGER ID 段。

## V1.3.2 共享账本

- 云端共享账本列表、下载和切换。
- OWNER / EDITOR / VIEWER 权限进入 APP。
- OWNER 可管理成员权限。
- APP 可使用注册码注册新账号。
- VIEWER 禁用编辑型入口，并且同步时只 pull 不 push。
- 云端镜像账本首次创建不注入默认数据。
- 修复新增记录产生重复 sync_change_log 的问题。
- DB_VERSION 10。

## V1.3.3 同步稳定版

- 启动、回前台、保存后自动同步，网络失败不阻塞本地记账。
- DB_VERSION 11：持久化同步冲突，可选采用云端/保留本机。
- 账本权限每次同步刷新；被移除后本机副本标记 REVOKED 并停止编辑。
- 增加云端 audit_log 修改记录查看。
- 主界面隐藏技术性 cursor，改名为同步序号并收进同步详情。

## V1.3.4 快速切账本 + 同设备多账号

- 首页左上角直接切换本机账本，当前账本置顶并显示权限。
- 同一台 Android 设备可登录不同云端账号，不再触发 device_id 409。
- 服务器设备 ID 改为“本机 device_id + 云端用户名”的 SHA-256。
- 账号切换时先锁定旧云端账本，再按新账号权限恢复，避免越权编辑。
- 继续兼容 TianXian Sync Server V1.0.4，无数据库迁移。

## V1.3.6 GitHub 自动更新

- 启动每 12 小时检查一次 `sockc/TianXianFruit` 最新 GitHub Release。
- 有新版时显示 Release 更新说明并可跳转 GitHub 下载。
- “更多”提供手动检查更新。
- Actions 支持 main 构建、`v*` Tag 自动 Release、网页手动发布 Release。
- Release APK 延续原签名，服务器仍为 V1.0.5-Lucky。

## V1.3.7 UI 分类与首页个性化

- “更多”改为分组式设置页，更新功能移入“关于天鲜账本”。
- 首页右上角齿轮直接进入顶部设置。
- 每个账本可本地设置顶部背景、主标题、副标题、右下角文案和水果装饰。
- 背景图片通过系统图片选择器导入 APP 私有目录，不申请相册存储权限。
- GitHub 自动更新与 V1.0.5-Lucky 云同步保持兼容。

## V1.3.8 SUPERADMIN 云同步修复

- SUPERADMIN 本地编辑可正常 push 到云端。
- SUPERADMIN / OWNER / EDITOR 统一使用云端写权限判断。
- 修复 SUPERADMIN 冲突处理与下载后 ID 区间初始化。
- 同步服务器继续使用 V1.0.5-Lucky。

## V1.3.9 经营统计重构

- 首页快捷操作改为经营统计、利润分配、生成报表、历史记录，并删除常用功能区。
- 更多页将历史记录和经营统计独立放入“账本与数据”，经营分析正式更名为经营统计。
- 经营统计升级为概览、趋势、摊位、客流、日历五个视图，并增加周期对比和异常提示。
- JSON 备份从统计页独立为“数据备份”。
- 去掉各主要表单日期/时间选择器上方的说明性介绍文字。

## V1.4.0 多人采购动态

- 正式进货单增加计划采购 / 临时采购协作标签。
- 新增按账本成员隔离的采购动态，VIEWER 可看，EDITOR/OWNER 可录入。
- 当天相同水果再次采购时提醒，但允许继续采购。
- 首页增加今日采购动态摘要；动态页每15秒刷新云端。
- 新增 purchase_activity 同步表，DB_VERSION=12，服务器继续兼容 V1.0.5-Lucky。
- GitHub Tag 冲突不再让整个构建失败：保留 signed Artifact，安全跳过重复 Release。

## V1.4.1 成员权限中心

- 云端共享账本与成员权限独立入口。
- 采购员 / 经营员 / 财务 / 只读 / 自定义权限模板。
- 未登录只显示登录页；普通用户无注册入口。
- 账号仅由 SUPERADMIN 创建；`REGISTRATION_CODE` 不再使用。
- 退出登录移动到更多页底部；更多页紧凑化。
- 需要配套 TianXian Sync Server V1.0.6-Lucky。

## V1.4.2 协作采购与高频录入优化
- 采购动态与采购计划合并为“协作采购”。
- 完成采购后才生成正式进货单。
- 进货取消计划/临时分类，单价与总价双向联动。
- 进货页面压缩为紧凑多行布局。
- 营业表当天已记录摊位顶置到日期下方。
- DB_VERSION=13，修复未知云端字段导致 SQLite 同步失败。
- 需要配套 Sync Server V1.0.7-Lucky。

