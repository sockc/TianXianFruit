## 天鲜账本 V1.4.2

### 协作采购
- 原“采购动态”改名为“协作采购”。
- 原独立“明日采购/采购清单”入口合并进协作采购，不再分成两个菜单。
- 协作采购按日期工作：今天、明天、以后都在同一个页面切换。
- 页面顶部直接显示：计划金额、已完成金额、已采购水果种类、剩余水果种类。
- 下方直接显示该日期的采购计划。
- 未完成项目可修改或移除；点击“完成采购”后填写实际数量、实际总价和进货人。
- **只有确认完成采购后，才自动生成正式进货单并计入当天进货金额。**
- 已完成项目保存实际数量、实际金额、进货人、完成账号和完成时间。
- 现场新增计划外水果，直接“＋添加水果”加入当天计划，不再区分“计划采购/临时采购”。

### 正式进货表
- 删除进货页面的“计划采购 / 临时采购”选择。
- 不再从采购计划手工导入已采购商品；协作采购完成时自动生成正式进货。
- 进货商品卡压缩为两行布局，一屏可看到更多水果。
- 删除按钮改成紧凑图标。
- **单价 ↔ 总价双向联动：**
  - 输入数量 + 单价，自动计算总价；
  - 输入数量 + 总价，自动反算单价；
  - 修改数量时，按最后一次手动修改的价格字段重新计算另一个字段。

### 营业表
- “当天已记录摊位”移动到营业日期正下方。
- 已记录摊位用横向紧凑卡片显示营业额和客户数。
- 点击已记录摊位直接载入当天记录继续修改。
- 不再需要滚到页面底部寻找当天记录。

### 云同步兼容修复
- 修复云端 `purchase_order.status` 存在而部分历史本地数据库缺少该列时的同步失败。
- DB V13 会自动补齐 `purchase_order.status`。
- 云端记录写入 SQLite 前会读取本地实际列名，自动忽略本地不存在的未知云端字段。
- 例如以后网页版比 APP 多出非关键字段，不再因为一个未知字段导致整条同步失败。
- 新增独立同步表 `purchase_collaboration`，避免直接给旧版 `purchase_plan_item` 增加协作字段，从而降低旧客户端同步兼容风险。

### 权限
- “查看采购动态”在界面中改名为“查看协作采购”，底层 permission key 继续使用 `purchase_activity.view` 以保持兼容。
- 编辑采购计划继续使用 `purchase_plan.edit`。
- 完成采购需要同时具备采购计划编辑权限和正式进货新增权限。

### 版本
- APP `versionCode 33`
- APP `versionName 1.4.2`
- Android `DB_VERSION 13`
- 配套 TianXian Sync Server `V1.0.7-Lucky`
- 建议参与协作采购的设备都升级到 V1.4.2 后再开始使用新协作流程。
